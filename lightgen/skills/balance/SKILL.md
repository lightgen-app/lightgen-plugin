---
name: lightgen-balance
description: Check your remaining LightGen image generations.
---

# LightGen Balance

Check the user's remaining image generations by calling the `mcp__lightgen__check_balance` tool.

Report the result clearly. If the balance is low (under 10), suggest purchasing more images:

```
npx @lightgen/cli setup --tier starter
```
